<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= blog.title %></title>
  <link rel="stylesheet" href="/css/bootstrap.min.css">
  <link rel="stylesheet" href="/css/font-awesome.all.min.css"> 
</head>
<body>
  <div class="container mt-5">
    <!-- Blog Post Card -->
    <div class="card">
      <div class="card-header">
        <h1 class="card-title"><%= blog.title %></h1>
      </div>
      <div class="card-body">
        <!-- Blog Body Content -->
        <p class="card-text"><%= blog.body %></p>
        <!-- Additional Blog Details -->
        <p class="card-text text-muted">Author: <%= blog.author.firstName %> <%= blog.author.lastName %></p>
        <p class="card-text text-muted">Read Count: <%= blog.readCount %></p>
        <p class="card-text text-muted">Posted on: <%= new Date(blog.createdAt).toDateString() %></p> 
      </div>
      <div class="card-footer text-right">
        <a href="/blogs" class="btn btn-primary">
          <i class="fas fa-arrow-left"></i> Back to Blogs
        </a>
      </div>
    </div>
  </div>

  <!-- Optional JavaScript -->
  <!-- jQuery first, then Popper.js, then Bootstrap JS -->
  <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/popper.js@1.9.1/dist/umd/popper.min.js"></script>
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
